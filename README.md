# python-delivery-system-simulator
Delivery Simulator is a Python command-line project that assigns packages to the nearest delivery agents, simulates routes, calculates distance and efficiency, and generates JSON/CSV reports. It also includes bonus features like random delays, ASCII route views, and best-performer summaries.
